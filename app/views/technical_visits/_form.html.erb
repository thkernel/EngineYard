<%= form_with(model: technical_visit, local: true) do |form| %>
  <% if technical_visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(technical_visit.errors.count, "error") %> prohibited this technical_visit from being saved:</h2>

      <ul>
      <% technical_visit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uid %>
    <%= form.text_field :uid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
