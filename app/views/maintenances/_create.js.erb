$("ul.errors").html("");
<% if @maintenance.errors.any? %>
  <% @maintenance.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".maintenances").html("<%= escape_javascript(render 'index') %>");
  $("#maintenance-modal").modal("hide");
 
<% end %>