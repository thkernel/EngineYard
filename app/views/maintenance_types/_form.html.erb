<%= form_with(model: maintenance_type, local: true) do |form| %>
  <% if maintenance_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(maintenance_type.errors.count, "error") %> prohibited this maintenance_type from being saved:</h2>

      <ul>
      <% maintenance_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uid %>
    <%= form.text_field :uid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
