$("ul.errors").html("");
<% if @engine_category.errors.any? %>
  <% @engine_category.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".engine-categories").html("<%= escape_javascript(render 'index') %>");
  $("#engine-category-modal").modal("hide");
 
<% end %>